version: "3.8"
services:
  db:
    image: postgres:14.1-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - '5433:5433'
    command: -p 5433
    volumes:
      - db:/var/lib/postgresql/data
#      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql

  redis-db:
    container_name: auth_redis
    image: redis:latest
    ports:
      - "6380:6380"
    command: redis-server --save 20 1 --loglevel warning --port 6380
    volumes:
      - redis-db:/data
#
#  web:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    container_name: auth_service
#    ports:
#      - "9090:9090"
#    depends_on:
#      - db
#    links:
#      - db

volumes:
  db:
    driver: local
  redis-db:
    driver: local